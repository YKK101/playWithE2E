import React from 'react'
import {
  View,
  StyleSheet,
} from 'react-native'

const Map = () => (
  <View
    testID="map"
    style={styles.container}
  />
)

const styles = StyleSheet.create({
  container: {
    flex: 1,
  }
})

export default Map